# Скопируйте этот файл как config.py и заполните своими значениями.
# config.py не попадает в Git (см. .gitignore).

# Токен группы VK (Управление сообществом → Работа с API)
TOKEN = ''

# ID группы (число из адреса или настроек)
GROUP_ID = 0
